#include<bits/stdc++.h>
using namespace std;

int main(){
    int t; cin >> t;
    while(t--) {
        int n, k;
        cin >> n>> k;

        vector<int>pos(n);

        for(int i = 0; i < n; i++)
            cin >> pos[i];

        
    }
    return 0;
}